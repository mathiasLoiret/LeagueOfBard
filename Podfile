# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'LeagueOfBard' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for LeagueOfBard
  pod 'Alamofire'
  pod 'AlamofireImage'
  pod 'SwiftyJSON'

  target 'LeagueOfBardTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'LeagueOfBardUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
